name:DFC
channels:
  - defaults
  - pytorch
dependencies:
    -jupyter=1.0.0
    -numpy=1.18.*
    -sklearn=0.0
    -torch=1.5.*
    -torchvision=0.6.*
    -pillow=7.1.*
    -scipy=1.4.*
    -matplotlib=3.1.*
    -pytorch-lightning=1.1.*
